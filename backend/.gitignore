.venv/
__pycache__/
*.py[co]
dist/

# Runtime data: the local stand-in for APP_DATA_DIR.
.appdata/

# Local environment, including a database password. Never in the zip either.
.env.local.sh

# The frontend build output. Produced by `npx next build` in ../frontend and copied
# here; see README. Kept out of git because it is a build artifact with the app id
# baked into every asset URL.
web/
